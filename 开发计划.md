# 文件暂存栈 —— 可执行开发计划

> 项目：Windows 拖拽暂存架（对标 macOS Yoink）
> 技术栈：C# + WPF，.NET 8
> 更新：2026-09-15

---

## 0. 一句话目标

MVP：拖动**文件**到屏幕侧边浮窗暂存 → 之后从浮窗把文件拖出，放到任意文件夹/应用。

---

## 里程碑总览

| 里程碑 | 名称 | 交付物 | 状态 |
|---|---|---|---|
| M0 | 环境与骨架 | 可编译运行的 WPF 空壳 | 待开始 |
| M1 | 侧边浮窗 | 贴边收放、半透明、置顶窗口 | 待开始 |
| M2 | 接收拖入（文件） | 拖动文件到浮窗 → 存入列表 | 待开始 |
| M3 | 拖出还原 | 从浮窗拖文件 → 放到目标位置 | 待开始 |
| M4 | 持久化 | 重启后暂存栈不丢 | 待开始 |
| M5 | 打磨 | 多选、快捷键、托盘、图标 | 待开始 |

---

## M0：环境与骨架（预计半天）

**目标**：一个能编译、能跑出窗口的 WPF 项目，确认工具链 OK。

**步骤**：
1. 确认本机已装 .NET 8 SDK（`dotnet --version`）。
2. `dotnet new wpf -n FileStash -o FileStash`
3. 建解决方案结构：
   ```
   FileStash/
   ├─ FileStash.sln
   ├─ FileStash/            # 主程序（WPF App）
   └─ FileStash.Core/       # 核心逻辑（暂存栈数据模型、存储）— 可后置
   ```
4. 跑 `dotnet run`，确认空窗口能弹出来。

**验收**：`dotnet run` 弹出窗口。

---

## M1：侧边浮窗（预计 1-2 天）

**目标**：一个贴屏幕右边缘、平时收起、鼠标靠近时滑出的半透明窗口。

**关键技术点**：
- `WindowStyle=None` + `AllowsTransparency=True` + `Background=Transparent` → 无边框透明窗。
- `Topmost=True` → 置顶，不抢焦点（`ShowActivated=False`）。
- 位置逻辑：窗口 Left = 屏幕工作区右边界 - 窄边宽度；收起时只露一条 10px 的触发条。
- 触发：`MouseEnter`（鼠标进入触发条）→ 滑出动画；`MouseLeave` → 收回。
- 多显示器：用 `SystemParameters.WorkArea` + `Screen.AllScreens`。

**验收**：右侧边缘有条触发带，鼠标划过去浮窗滑出，划走收回。

---

## M2：接收拖入文件（预计 2-3 天）—— 核心难点

**目标**：把文件从资源管理器拖到浮窗上，浮窗接收并显示文件名。

**关键技术点**：
1. 浮窗根元素设置 `AllowDrop=True`。
2. 处理 `DragOver` / `DragEnter`：检测 `e.Data.GetDataPresent(DataFormats.FileDrop)` → 设 `e.Effects = Copy`。
3. 处理 `Drop`：`e.Data.GetData(DataFormats.FileDrop)` 得到 `string[]` 路径。
4. 存入内存列表（`ObservableCollection<StashItem>`），绑定到 `ListBox` 显示。

> ⚠️ 说明：WPF 自带 `AllowDrop` 只能接收「拖到窗口上再放下」。
> **「拖动过程中自动弹出浮窗」需要在 M5 用全局鼠标钩子实现**，MVP 先做「窗口常驻、拖进去放下」这个更稳的路径。

**验收**：从文件夹拖一个文件到浮窗，浮窗列表里出现该文件名。

---

## M3：拖出还原（预计 2-4 天）—— 核心难点

**目标**：从浮窗列表里把暂存的文件拖出，放到目标文件夹/应用。

**关键技术点**：
1. 在 `ListBoxItem` 上监听 `PreviewMouseLeftButtonDown` + `MouseMove`，检测拖拽阈值 → 启动拖拽。
2. 用 `DataObject(DataFormats.FileDrop, string[])` 构造数据对象。
3. `DragDrop.DoDragDrop(item, dataObj, DragDropEffects.Copy)`。
4. 目标是文件夹时，资源管理器会收到 `CF_HDROP` 完成复制。
5. 拖出后可选：从暂存栈删除（"取出即清空"）或保留（"暂存副本"），需定交互。

**验收**：从浮窗拖一个文件到桌面文件夹，文件被复制过去。

---

## M4：持久化（预计 1 天）

**目标**：重启程序后暂存栈列表还在。

**方案**：把暂存列表序列化成 JSON 存到 `%AppData%\FileStash\stash.json`，启动时读回。
- 存字节路径 + 名称 + 图标索引。
- 启动时校验路径是否仍有效，失效的标灰。

**验收**：放几个文件进栈，重启程序，列表还在。

---

## M5：打磨（按需）

- **全局鼠标钩子自动弹出**（最难，可选）：`SetWindowsHookEx(WH_MOUSE_LL)` 监听左键拖拽，判断是否携带文件 → 鼠标靠近边缘时滑出浮窗。这一步让体验真正接近 Yoink。
- **多选拖入/拖出**。
- **系统托盘图标 + 全局快捷键**（如 `Ctrl+` 呼出）。
- **文件图标缩略图**。
- **文字 / 图片暂存**（数据源 `CF_TEXT` / `CF_BITMAP`，后置）。

---

## 待定交互决策（开发前需用户拍板）

1. 浮窗形态：**贴边自动弹出** vs **常驻窄边**？（MVP 建议常驻，M5 再升级为自动弹出）
2. 拖出后：**取出即清空** vs **保留副本**？
3. 浮窗位置：固定右侧，还是左右可切换、跟随鼠标所在屏？
4. 暂存同名文件 / 文件夹是否合并？

---

## 风险与预案

| 风险 | 说明 | 预案 |
|---|---|---|
| 拖出还原兼容性 | 不同目标应用对 `CF_HDROP` 处理不一 | 先只支持拖到资源管理器文件夹，再扩展 |
| 全局钩子（M5） | low-level 钩子复杂、易误触发 | 独立成可开关的进阶功能，MVP 不阻塞 |
| 权限/UAC | 拖到系统受保护目录需提权 | MVP 引导用户拖到用户可写目录 |
| 透明窗口焦点 | 透明+置顶易抢焦点或点击穿透 | `ShowActivated=False` + 事件穿透处理 |
