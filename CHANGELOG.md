# 更新日志

所有值得注意的版本变更都记录在此。

## [v1.0.5] - 2026-09-18

### 修复
- **偶发卡死 / 任务栏闪图标后消失**
  - 网页图片拖入时改为后台异步下载，不再在 UI 线程同步等待（原来网络慢或超时会冻结整个窗口 15 秒，被系统判定为“未响应”，并连带卡住全局鼠标）
  - 拖入内容解析（`new Uri` 等）加异常保护，非法字符不再导致程序崩溃
  - 全局鼠标钩子回调加节流与异常兜底，避免高频消息淹没 UI 队列
  - 新增单实例保护与全局异常兜底，偶发异常不再直接退出进程
  - 修复高 DPI（如 200% 缩放）下“靠近边缘”判定区被放大到半个屏幕的问题：鼠标钩子的物理像素坐标统一换算为 DIP 再比较
- **高刷新率屏幕（如 240Hz）上滑入/滑出动画帧率偏低**
  - 收/展动画由「动画窗口位置 `Window.Left`」改为「动画内容 `RenderTransform`」：窗口本体不再移动，不触发每帧 `SetWindowPos`，动画改由渲染线程合成并跟随显示器刷新（硬件加速下自然跟随 60/120/144/240Hz 屏幕）

### 说明
- 透明区域（收起时主面板部分）依赖分层窗的 alpha 穿透，点击可正常穿透到桌面，不阻挡操作
- 涉及拖拽/触发灵敏度与动画流畅度，建议在目标机器上实测

## [v1.0.4] - 2026-09-16

### 新增
- **双击打开文件**：双击列表项用系统默认程序打开该文件
- **空格 QuickLook 预览**：选中项按空格弹出预览窗，图片显示原图+分辨率+大小，其他文件显示大图标+类型/大小/修改时间/路径；Esc/空格/点击关闭

### 变更
- **触发灵敏度**：面板弹出条件改为「按住左键拖动 + 鼠标靠近停靠侧边缘」，阈值经多轮实测定为 15px（既避免选中文字误弹，又便于拖文件时触发）

## [v1.0.3] - 2026-09-15

### 新增
- **网页图片拖拽暂存**：支持从网页直接拖图片到面板
  - 识别 HTML 格式（网页图片拖拽主流格式）
  - 优先提取 `mediaurl=` 原图直链（必应/百度结果页，拿到高清原图而非缩略图）
  - `srcset`/`data-src` 高清候选优先
  - 图片 URL 文本自动识别下载、`file://` 本地直链

### 修复
- 拖纯文字时错误地走了 HTML 分支导致存成富文本源码：调整拖入优先级，纯文字优先于 HTML

## [v1.0.2] - 2026-09-15

### 新增
- **左右停靠切换**：面板可在屏幕左/右侧停靠，偏好持久化

### 变更
- 触发灵敏度调低（30px → 8px），不再一碰就弹
- 整体界面缩小（320×600 → 272×512）

## [v1.0.1] - 2026-09-15

### 变更
- 打磨与稳定性改进

## [v1.0.0] - 2026-09-15

### 新增
- 首个正式版本：侧边浮窗暂存架，支持拖入/拖出文件、文字、图片，持久化、托盘、快捷键、多选、图标缩略图等核心功能
