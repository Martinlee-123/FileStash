# 文件暂存栈 —— 项目交接文档

> 给下一个对话(新会话)看的完整交接说明。接手本任务前**先读这一份**。
> 最后更新:2026-09-15 19:45

---

## 🚀 新会话快速上手(先看这段)

**当前状态**:功能已全部完成并实测通过(M0~M7)。剩下只有可选项(GitHub 上传、多显示器等)。
**用户日常使用**:双击桌面 `文件暂存栈-启动.lnk` 启动。

**编译运行三步**(⚠️ PowerShell,不是 bash):
```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
cd "C:\Users\marti\Desktop\文件暂存栈\FileStash"
dotnet build -c Debug
Start-Process .\bin\Debug\net8.0-windows\FileStash.exe
```
**⚠️ 改代码前先停进程**(否则 exe 被锁编译失败):`Get-Process FileStash -ErrorAction SilentlyContinue | Stop-Process -Force`
**⚠️ 启动后必看是否崩**:启动等 8 秒,若进程消失 = XAML/代码抛异常,跑 `& .\bin\Debug\net8.0-windows\FileStash.exe` 看控制台报错。

**用户验证方式**:用户看得到的是屏幕右边缘窄条 / Ctrl+Alt+S / 托盘图标。需用户手动拖拽验证的功能,改完要请用户实测。

---

## 0. 一句话定位

Windows 平台**系统级拖拽暂存架**,对标 macOS 的 Yoink:
选中/拖动文件时屏幕侧边弹出浮窗,把文件丢进去暂存,需要时再拖出到任意位置。

技术栈:**C# + WPF,.NET 8**(README 里「推荐技术栈」已定死,别换)。

---

## 1. 当前进度:✅ MVP 完成 + M5 完成 + M6 文字/图片暂存完成

| 里程碑 | 名称 | 状态 |
|---|---|---|
| M0 | 环境与骨架 | ✅ 完成 |
| M1 | 侧边浮窗(贴边收放/半透明/置顶) | ✅ 完成 |
| M2 | 接收拖入文件 | ✅ 完成 |
| M3 | 拖出还原 | ✅ 完成 |
| M4 | 持久化(重启不丢) | ✅ 完成 |
| M5 | 打磨(图标/多选/托盘/快捷键) | ✅ 完成 |
| M6 | 文字/图片暂存(CF_TEXT/CF_BITMAP) | ✅ 完成 |
| M7 | 配色重做 + 交互打磨 | ✅ 完成 |

**已全部完成的功能:**
- ✅ 右键删除单个项 + 底部「清空」按钮
- ✅ 「找回」功能(清空前快照到 `stash.recycle.json`，一键恢复)
- ✅ 文件图标缩略图(16x16 系统 shell 图标, `FileIconConverter.cs`)
- ✅ 多选(`SelectionMode=Multiple`，点击即 toggle，前面有 CheckBox 打勾)
- ✅ 多选拖入/拖出
- ✅ 系统托盘图标 + 全局快捷键(Ctrl+Alt+S)
- ✅ 「固定」开关:标题栏 ToggleButton,开启后面板常驻展开、鼠标离开不收回
- ✅ 「纯文字」开关:标题栏 ToggleButton,开启后拖出单个 txt 时以纯文字形式(适配 QQ/微信)
- ✅ 文字暂存:选中文字拖入 → 自动生成临时 `.txt`
- ✅ 图片暂存:选中图片拖入 → 自动生成临时 `.png`
- ✅ 面板有文件时强制保持展开(状态驱动,详见 5.9)
- ✅ 浅灰半透明玻璃质感配色 + 圆角按钮(详见 5.10)
- ✅ 桌面快捷方式 `文件暂存栈-启动.lnk`
- ✅ 拖入后自动保持展开(不缩回),无需再唤醒

**尚未做:**
- ❌ GitHub 上传(用户有意向,待拍板,见第 7 节)
- ❌ 多显示器支持 / UAC 目录 / 拖出被识别为「移动」等边界问题(见第 8 节)

**已实测通过:**
- 拖文件靠近屏幕右边缘 → 面板自动弹出(全局鼠标钩子)
- 丢进面板 → 文件名入列表
- 从列表拖出 → 复制到目标位置,暂存项删除(拖出即取出)
- 重启后列表读回(stash.json),失效路径自动跳过
- 拖入文字/图片 → 生成临时文件;拖出到 QQ 显示文字/文件
- 拖入后鼠标移开 → 面板保持展开

**已实测通过:**
- 拖文件靠近屏幕右边缘 → 面板自动弹出(全局鼠标钩子)
- 丢进面板 → 文件名入列表
- 从列表拖出 → 复制到目标位置,暂存项删除(拖出即取出)
- 重启后列表读回(stash.json),失效路径自动跳过

---

## 2. 已定的交互决策(用户拍板,勿改)

1. 浮窗形态:**常驻窄边**(非贴边自动隐藏,触发条常驻)
2. 拖出后:**拖出即取出**——拖出成功(目标接受)即删除暂存项,避免反复复制同一文件。⚠️ 2026-09-15 用户拍板变更,旧版「保留副本」已废弃
3. 浮窗位置:**固定右侧**(主屏工作区右边缘)
4. 同名文件:**允许重复**(不做合并/去重)
5. **面板有文件时不自动缩回**——`Collapse()` 里 `StashItems.Count>0` 直接 return;清空后(空列表)才恢复鼠标离开收回
6. 多选交互:**单击切换选中**(松手才生效,不是按下即切),拖动不影响选中;拖出/右键删除作用于整个选中集合
7. **「固定」开关**:标题栏右侧 ToggleButton,开启后 `Collapse()` 直接 return(面板常驻展开),关闭后恢复原逻辑。固定时按钮显示「已固定」
8. **「纯文字」开关**:标题栏 ToggleButton,**默认关闭**——关闭时拖出 txt = 文件格式(可拖桌面/资源管理器);开启时拖出单个 txt = 纯文字格式(QQ/微信直接显示文字)。开启时按钮显示「文字中」。⚠️ 两种格式**不能共存**:桌面要文件格式、QQ 显示文字要纯文字格式,因此必须用开关让用户选
9. **面板展开保持**:只要有文件就不收回(状态驱动,不看鼠标位置),详见 5.9

---

## 3. 环境

- **.NET SDK**:8.0.425,已通过 winget 安装(`Microsoft.DotNet.SDK.8`)
- 本机原无 dotnet,是本次会话装的。新机器需先装 .NET 8 SDK
- **编译命令**(⚠️ 必须带上 Machine+User 的 PATH,否则 dotnet 找不到):
  ```powershell
  $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
  cd "C:\Users\marti\Desktop\文件暂存栈\FileStash"
  dotnet build -c Debug
  ```
- **运行**:`Start-Process .\bin\Debug\net8.0-windows\FileStash.exe`
- **停进程**:`Get-Process FileStash | Stop-Process -Force`
- 终端是 PowerShell(不是 msys/bash,`ls -la` 会报错,用 `Get-ChildItem`)

---

## 4. 文件结构

```
C:\Users\marti\Desktop\文件暂存栈\
├─ README.md                  # 项目定位 + 竞品调研(不变)
├─ 开发计划.md                # 原始里程碑计划(不变)
├─ 交接文档.md                # 本文件
└─ FileStash\                 # 主项目(WPF App,.NET 8)
   ├─ FileStash.csproj
   ├─ App.xaml / App.xaml.cs
   ├─ AssemblyInfo.cs
   ├─ StashItem.cs            # 数据模型:Path + Name + IsTemporary
   ├─ FileIconConverter.cs    # 文件图标转换器(shell 图标 → ImageSource)
   ├─ TrayIcon.cs             # 系统托盘图标(浅灰底自绘)
   ├─ HotKeyManager.cs        # 全局快捷键 Ctrl+Alt+S
   ├─ MainWindow.xaml         # UI:触发条 + 主面板 + 列表 + 四个圆角开关/按钮(含样式定义)
   └─ MainWindow.xaml.cs      # 全部逻辑(收放/钩子/拖入/拖出/删除/找回/持久化/托盘/快捷键/文字图片暂存)
```

**桌面快捷方式**:`C:\Users\marti\Desktop\文件暂存栈-启动.lnk`(指向 `bin\Debug\net8.0-windows\FileStash.exe`)
- ⚠️ 桌面还有一个**同名文件夹** `文件暂存栈`,所以快捷方式不能叫「文件暂存栈」(Windows 隐藏 .lnk 扩展名后会和文件夹混淆),已命名「文件暂存栈-启动」区分
- ⚠️ 快捷方式名字不要带 emoji(WScript.Shell 解析 emoji 文件名会失败)

**注意**:`bin/`、`obj/` 是生成物,git 提交时忽略。

---

## 5. 关键实现细节(踩坑记录,勿推翻)

### 5.1 触发条位置(M1 的重大坑)
- 触发条必须放窗口**最左列**,主面板在它**右边**。
- 因为窗口整体向右移出屏幕,若触发条放最右,露出的 10px 是主面板左边缘,触发条被推到屏幕外 → **看不到也触发不了**。
- 当前 XAML 列顺序:`[触发条 10px] [主面板 *]`,触发条 `Grid.Column=0`。

### 5.2 浮窗收放逻辑
- `WindowStyle=None` + `AllowsTransparency=True` + `Background=Transparent` → 无边框透明窗
- `Topmost=True` + `ShowActivated=False` → 置顶且不抢焦点,`ShowInTaskbar=False`
- 收起时 `Left = 工作区右边界 - 10px`;展开时 `Left = 工作区右边界 - 320px`
- 垂直居中:`Top = 工作区顶 + (工作区高 - 窗口高)/2`
- 用 `SystemParameters.WorkArea`(注意不是 `VirtualScreen`,只取主屏工作区)
- 动画:`DoubleAnimation` 180ms + `CubicEase EaseOut`,动画结束后 `Left = target` 兜底
- **鼠标滑入触发条 → `Expand`;鼠标离开整个 Grid → `Collapse`**(`RootGrid_MouseLeave`)

### 5.3 全局鼠标钩子(拖拽自动弹出,核心难点)
- `SetWindowsHookEx(WH_MOUSE_LL=14)` 低层鼠标钩子,`GetModuleHandle(null)` 挂当前进程
- 监听 `WM_MOUSEMOVE=0x0200`:`GetAsyncKeyState(VK_LBUTTON=0x01)` 判断左键按住 + 鼠标 X 坐标 ≥ `工作区右边界 - 30px`(EdgeThreshold)→ `Dispatcher.BeginInvoke(Expand)`
- 监听 `WM_LBUTTONUP=0x0202`:松开时鼠标不在窗口内 → 收回
- 钩子回调里**必须用 `Dispatcher.BeginInvoke`** 切回 UI 线程操作窗口
- 关闭窗口时 `UnhookWindowsHookEx` 卸载,防泄漏
- `EdgeThreshold = 30`(像素),太灵敏/迟钝可调这个常量

### 5.4 接收拖入(M2)
- 主面板和 ListBox 都设 `AllowDrop=True`,处理 `DragEnter`/`DragOver`/`Drop`
- 检测 `e.Data.GetDataPresent(DataFormats.FileDrop)` → 是则 `e.Effects = Copy`,否则 `None`
- `Drop` 里 `e.Data.GetData(DataFormats.FileDrop)` 得到 `string[]` 路径
- **只收文件 `File.Exists(path)`,MVP 先不收文件夹**

### 5.5 拖出还原(M3,含多选)
- 拖拽事件**用 `ItemContainerStyle + EventSetter` 绑到每个 ListBoxItem**(`ListItem_PreviewMouseLeftButtonDown/Move/Up`),不要绑到 StashList 上——Multiple 模式下 ListBoxItem 鼠标按下会捕获鼠标,StashList 的 PreviewMouseMove 收不到事件
- 阈值用 `SystemParameters.MinimumHorizontalDragDistance` / `MinimumVerticalDragDistance`
- **选中切换是「松手才切」的手动实现**:MouseDown 时 `e.Handled=true` + `CaptureMouse`,MouseUp(未拖动)才 `ToggleSelection`。因为 Multiple 模式默认「按下即切」,会导致一按下去选中就丢
- 拖出集合:`_dragSnapshot`(按下时快照)+ `_dragPivotItem`(按下命中项),锚定项在多选中则拖出全部选中项
- 构造 `DataObject(DataFormats.FileDrop, validPaths)` 还原 `CF_HDROP`
- `DoDragDrop(StashList, dataObj, Copy)`,返回非 None 视为成功
- **拖出即取出**:`result != None` 时 `StashItems.Remove` 拖出的项
- 拖出中用 `_dragOutSuppressCollapse` 防止面板中途收回

### 5.6 持久化(M4)
- 路径:`%AppData%\FileStash\stash.json`(即 `C:\Users\marti\AppData\Roaming\FileStash\stash.json`)
- `System.Text.Json` 序列化,`WriteIndented = true`
- 列表 `CollectionChanged` 时自动保存 + 窗口 `Closed` 时保存
- 启动 `LoadStash` 读回,`File.Exists` 校验失效路径跳过
- 保存/读取都 try-catch,失败不阻断

### 5.7 删除/清空/找回(M5)
- 右键菜单 `ContextMenuOpening` 定位 `_contextMenuItem`;「删除此项」只在命中具体项时显示
- **多选删除**:右键指向项在多选中时删除全部选中项
- 「清空」先把快照存到 `stash.recycle.json`,再 `Clear`
- 「找回」读 `stash.recycle.json` 加回列表,找回后删除快照;仅「清空」触发快照,单条删除不触发
- **右键菜单打开时面板不缩回**(`RootGrid_MouseLeave` 里判断 `ContextMenu.IsOpen`);菜单关闭后鼠标在面板外才收回

### 5.8 文件图标(M5)
- `FileIconConverter.cs`:`SHGetFileInfo(SHGFI_ICON|SHGFI_SMALLICON)` 提取 16x16 shell 图标
- ⚠️ **大坑**:`SHGFI_ICON` 模式下 `SHGetFileInfo` 的返回值只是成功标志,**真正的图标句柄在 `shfi.hIcon` 字段**,别把返回值当句柄(会报「无效的光标句柄」崩溃)
- `CreateBitmapSourceFromHIcon` 转 ImageSource + `Freeze()`;带按路径内存缓存

### 5.9 面板展开保持(重大交互,2026-09-15 晚)
**需求**:拖入文件后鼠标移开,面板要保持展开不缩回,无需再唤醒一次。
**坑**:第一版靠 `Collapse()` 里 `Count>0` 拦截,但**时序不定**——拖入较慢时 `MouseLeave` 早于 `Drop` 执行,那时列表还是空的,就收回了。800ms 时间抑制也不够。
**最终方案(状态驱动,勿改回时序抑制)**:
- `StashItems.CollectionChanged` 里:一旦 `Count>0` 就 `Dispatcher.BeginInvoke(Expand)` —— **只要有内容就强制展开**(就算中途被误收也马上弹回)
- `RootGrid_MouseLeave` 里:有文件 / `_isPinned` 时**直接 return**,根本不调收起
- `Collapse()` 里:`_isPinned` 或 `Count>0` 直接 return
- 保留 `_lastDropTime` + 800ms 抑制作为额外保险
- 不变式:**列表有内容 = 面板保持展开**

### 5.10 配色/样式(M7,2026-09-15 晚)
- **主面板**:浅灰半透明玻璃 `#E6DADAE0` + 白色描边;触发条 `#DCE8E8EC`
- **标题/列表文字**:深色(`#1E1E24` / `#2A2A32`)
- **开关/按钮样式**(XAML `Window.Resources`,代码不再设颜色):
  - `ToggleChipStyle`(TargetType=**ToggleButton**):浅白底 `#F2FFFFFF` + 深字 + 圆角 6;悬停纯白;选中变浅绿(`#B8ECC6` 底 + `#FF1B6E3A` 字)
  - `ChipButtonStyle`(TargetType=**Button**):同款外观,给「找回」「清空」用
- ⚠️ **大坑(曾导致启动即崩)**:把 `TargetType=ToggleButton` 的样式套到 `Button` 上 → `XamlParseException: TargetType 与元素类型不匹配` → **程序启动秒退、无任何窗口、托盘也无图标**。这就是「双击打不开」的真因。**ToggleButton 和 Button 必须各用各的样式**。
- **托盘图标**:浅灰圆角底 + 深色三横线(自绘)
- 列表项选中高亮:`#4080B0FF` 淡蓝

### 5.11 文字/图片暂存(M6,2026-09-15 晚)
- **拖入判定** `IsStashable(data)`:FileDrop / UnicodeText / Text / Bitmap 任一即可
- **Drop 优先级**:文件 > 文字 > 图片位图
- **文字** → `AddTextToStash`:存为 `%AppData%\FileStash\tmp\<前24字>_<HHmmss>_<4位>.txt`
- **图片** → `AddBitmapToStash`:`PngBitmapEncoder` 存为 `tmp\截图_<HHmmss>_<4位>.png`
- 两者都标 `StashItem.IsTemporary = true`
- **临时文件生命周期**(⚠️ 坑):
  - **拖出/删除/清空时不能立即删**!QQ 等是**异步读文件**,`DoDragDrop` 一返回就删 → 目标读到 0 字节 → 报「自动过滤空文件」。已改**延迟删除**:入 `_pendingDelete`,`DispatcherTimer` 15 秒后统一删(`ScheduleTempDelete` / `FlushPendingDelete`)
  - 真实文件(用户从磁盘拖入)**永不碰磁盘**,只从列表移除
  - 启动时 `CleanupOrphanTempFiles()` 清理 tmp 里不在列表中的孤儿文件
- **拖出格式(双开关,见决策 8)**:默认拖出=文件(`FileDrop`);「纯文字」开关开+单个 txt=纯文字(`UnicodeText`+`Text`,**不带 FileDrop**,否则 QQ 优先当文件)
- ⚠️ **坑:拖出后被丢回自己面板会复制出相同文件**。`MainPanel_Drop` 开头判断 `_dragOutSuppressCollapse`(拖出中)→ 直接 return

---

## 6. 待办

### 6.1 已完成
- ~~系统托盘图标 + 全局快捷键~~ ✅
- ~~文字/图片暂存(CF_TEXT/CF_BITMAP)~~ ✅
- ~~配色重做~~ ✅
- ~~拖入后保持展开~~ ✅

### 6.2 托盘 + 快捷键实现备忘(2026-09-15)
- **托盘**:`TrayIcon.cs`,用 WinForms `NotifyIcon`(项目已开 `UseWindowsForms`),运行时用 `Graphics` 自绘 16x16 图标(`GetHicon` + `Icon.FromHandle` + `Clone`,用完 `DestroyIcon`)。右键菜单:显示/隐藏浮窗、清空、找回、快捷键提示、退出。双击托盘=切换浮窗。
- **全局快捷键**:`HotKeyManager.cs`,`RegisterHotKey(Ctrl+Alt+S)` + `HwndSource.AddHook` 捕获 `WM_HOTKEY`(id=0x4653)。
- **切换逻辑**:`TogglePanel()` 隐藏时 `UninstallMouseHook()` + `Hide()`;显示时 `Show()` + `PositionToRightEdge()` + `SetCollapsed()` + `InstallMouseHook()`。隐藏时鼠标钩子会卸载,否则边缘自动弹出会和「隐藏」语义冲突。
- **退出**:托盘菜单「退出」走 `ExitApplication()`(SaveStash + UninstallMouseHook + `Application.Current.Shutdown()`),这是唯一优雅退出入口(窗口无边框无关闭按钮)。
- ⚠️ **坑:开 `UseWindowsForms` 后命名冲突**:`Application`/`Point`/`DataFormats`/`DataObject` 在 `System.Windows.Forms` 和 `System.Windows` 之间二义,报 CS0104。已在 `MainWindow.xaml.cs` 全限定为 `System.Windows.*`;`App.xaml.cs` 基类改成 `System.Windows.Application`。`TrayIcon.cs` 内部用 `using System.Windows.Forms;` + `System.Drawing`,不冲突。
- ⚠️ **坑:编译时 exe 被运行中进程锁定**(MSB3026/3027「being used by another process」):先 `Get-Process FileStash | Stop-Process -Force` 再 `dotnet build`。
- ⚠️ **现象:重启后偶现「2 个 FileStash 进程」**,其中一个是 StartTime 为空的退出残留句柄(非真双开)。等几秒自消;或 `taskkill /F /IM FileStash.exe` 清干净。

### 6.3 可做(待用户拍板)
- GitHub 上传(见第7节)
- 多显示器支持 / UAC 目录 / 拖出被识别为「移动」等边界优化(见第8节)

---

## 6.4 免安装分发版(2026-09-15 晚，已完成)

**需求**:别人的电脑不装 .NET 8 也能双击直接用。

**方案**:自包含发布(self-contained)+ 单文件 + 压缩。

**发布命令**(⚠️ 先停进程，PATH 见第9节):
```powershell
cd "C:\Users\marti\Desktop\文件暂存栈\FileStash"
dotnet publish -c Release -r win-x64 --self-contained true `
  -p:PublishSingleFile=true `
  -p:EnableCompressionInSingleFile=true `
  -p:IncludeNativeLibrariesForSelfExtract=true `
  -o "C:\Users\marti\Desktop\文件暂存栈\发布版"
```

**产物**:
- `发布版\FileStash.exe` —— 单文件 **68.3MB**(内含 .NET 8 WPF 运行时)
- `发布版\使用说明.txt` —— 给最终用户的操作说明
- `FileStash_免安装版_win-x64.zip` —— **62.97MB**,分发包(压缩后)

**已验证(三重)**:
1. 直接 Start-Process 启动成功，进程路径 = 发布版 exe
2. 进程模块检查:运行时 DLL(`PresentationNative_cor3.dll`/`wpfgfx_cor3.dll` 等)**从 `%LocalAppData%\Temp\.net\FileStash\...` 解压加载** → 单文件自包含特征;✅ **零**模块来自 `C:\Program Files\dotnet`
3. 极简环境(清空 DOTNET_* 变量 + PATH 仅 System32)启动成功 → 证明不依赖系统 .NET

**原理**:单文件 exe 内含完整运行时，首次启动会解压到 `%LocalAppData%\Temp\.net\` 再运行，因此**首启稍慢**属正常。
**兼容**:Windows 10/11 64位(x64)。ARM 设备需另出 `win-arm64` 包。
**注意**:exe 无签名，SmartScreen 可能提示「未知发布者」→ 点「更多信息」→「仍要运行」。

---

## 7. GitHub 上传(用户有意向，尚未开始)

- 用户 2026-09-15 提过「想上传 GitHub」,**但还没动手**。
- 待用户拍板的点:**公开 vs 私有**、**License(MIT/GPL/无)**、**repo 名称**。
- 动手时要做:本地 `git init` + `.gitignore`(排除 `bin/`、`obj/`、`.vs/`)+ 首次 commit + `git remote add` 推送。
- 项目本身(纯 C# 源码)非常适合 GitHub,后续可加 GitHub Actions 做 `dotnet build` CI。

---

## 8. 已知边界/风险(未解决,注意)

- **拖出到不同目标应用,`CF_HDROP` 处理不一**:有的会变成「移动」而非「复制」。若发现文件被移走(原路径消失),需在 `DoDragDrop` 的 effect 处理上强化 Copy 语义。
- **多显示器**:当前只贴主屏右边缘(`SystemParameters.WorkArea`),未处理副屏。
- **拖到系统受保护目录**(需 UAC 提权):MVP 未处理,引导用户拖到用户可写目录。
- **透明+置顶窗口焦点**:已用 `ShowActivated=False` 缓解,但可能有边缘点击穿透问题。

---

## 9. 快速上手命令速查

```powershell
# 1. 准备 PATH(dotnet 命令必需)
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

# 2. 编译
cd "C:\Users\marti\Desktop\文件暂存栈\FileStash"
dotnet build -c Debug

# 3. 运行
Start-Process .\bin\Debug\net8.0-windows\FileStash.exe

# 4. 停止(⚠️ 若 Stop-Process 报 Access denied,用 taskkill 强制)
Get-Process FileStash -ErrorAction SilentlyContinue | Stop-Process -Force
taskkill /F /IM FileStash.exe

# 5. 查看持久化数据
Get-Content "$env:APPDATA\FileStash\stash.json" -Raw
```

**启动后确认单实例**:`(Get-Process FileStash).Count` 应为 1;若出现 2 个(其中一个空 StartTime/Path),是崩溃残留的僵死句柄,用 `taskkill /F /IM FileStash.exe` 清干净再启动。
